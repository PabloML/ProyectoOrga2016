#include "pila.h"
#include "lista.h"
#include <stdio.h>
#include <stdlib.h>

/*int main()
{

    //Probando una lista
     lista_t l=lista_crear();
    lista_insertar(l,0,0);
    lista_insertar(l,1,1);
    lista_insertar(l,2,2);
    lista_insertar(l,3,3);
    lista_insertar(l,4,4);
    lista_insertar(l,5,5);
    lista_insertar(l,6,6);
    lista_insertar(l,7,7);

    lista_eliminar(l,7);//ultimo elemento y a la vez unico en la celda.
    lista_eliminar(l,6);//primer elemento.
    lista_eliminar(l,4);//elemento intermedio.
    lista_eliminar(l,3);

    lista_destruir(&l);



    pila_t pila=pila_crear();
    apilar(&pila,"Pablo");
    printf("El tope de la pila es: %s\n",tope(pila));
    apilar(&pila,"Sofia");
    printf("El tope de la pila es: %s\n",desapilar(&pila));
    printf("El tope de la pila es: %s\n",desapilar(&pila));

    return 0;
}*/
